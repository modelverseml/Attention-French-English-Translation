# raw Europarl corpus (large; download separately)
fr-en/
# python / model caches
__pycache__/
*.py[cod]
.venv/ venv/
# node
node_modules/
frontend/dist/
# NOTE: data/model_artifacts/ is intentionally committed so the Streamlit
# deploy can load the trained models (each file is well under GitHub's 100 MB).
